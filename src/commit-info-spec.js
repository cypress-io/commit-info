'use strict'


/* eslint-env mocha */
const commitInfo = require('.')

describe('commit-info', () => {
  it('write this test', () => {
    console.assert(commitInfo, 'should export something')
  })
})
